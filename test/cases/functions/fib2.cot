// EXPECT: exit_code=1
// fib(2) = fib(1) + fib(0) = 1 + 0 = 1
fn fib(n: int) int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() int {
    return fib(2);
}
