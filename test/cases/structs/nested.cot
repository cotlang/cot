// EXPECT: exit_code=6
struct Inner {
    value: int,
}

struct Outer {
    inner: Inner,
    extra: int,
}

fn main() int {
    let o: Outer = Outer {
        .inner = Inner { .value = 3 },
        .extra = 3
    };
    return o.inner.value + o.extra;
}
