enum CeColor {
    Red,
    Green,
    Blue
}

enum CeStatus {
    Ok = 0,
    Warning = 50,
    Error = 100
}

test "enum simple" {
    var c = CeColor.Green
    @assert_eq(c, 1)
}

test "enum explicit value" {
    var s = CeStatus.Error
    @assert_eq(s, 100)
}
