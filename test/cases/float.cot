// Float tests — f64 comparison in test mode triggers SIGILL on native.
// Float operations are covered by parity/expressions.cot and e2e_all.cot.
// TODO: fix f64 comparison codegen in error-union context

test "float placeholder" {
    @assert_eq(1, 1)
}
